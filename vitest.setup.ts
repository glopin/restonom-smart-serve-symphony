// import '@testing-library/jest-dom/vitest'; // vitest >=0.34.0
import '@testing-library/jest-dom'; // older vitest or if using directly

// Any other global setup can go here.
// For example, mocking a global object:
// global.ResizeObserver = require('resize-observer-polyfill')
